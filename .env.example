# Database (Prisma / Postgres)
# Connect to your Supabase or Railway postgres instance
DATABASE_URL="postgresql://user:password@host:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://user:password@host:5432/postgres"

# NextAuth.js
# Generate a secret via 'openssl rand -base64 32' or similar
NEXTAUTH_SECRET="your-super-secret-key-at-least-32-chars"
NEXTAUTH_URL="https://your-domain.com"

# Redis (Upstash / Railway) - Optional but Recommended for Scaling
# If fetching from simple local hub, leave blank (but limited to 1 instance)
# For production scalable hub, provide Rediss URL
REDIS_URL="rediss://default:password@your-redis-host:6379"
HUB_INSTANCE_ID="node-1"

# Hub Tuning
HUB_TOP_N=50
HUB_DEBOUNCE_MS=100

# Cache Tuning
LEADERBOARD_CACHE_MS=15000
ENABLE_LEADERBOARD_CACHE=true

# Feature Flags
NEXT_PHASE="phase-production-build"
